import { Typography } from "@mui/material";

const MsgLogsWidget = ({}) => {

    return(
        <div style={{backgroundColor: "brown", height: "100%", width: "auto", display: "block"}}>
            <Typography>select a message to view chat log.</Typography>
        </div>
    )




}

export default MsgLogsWidget;